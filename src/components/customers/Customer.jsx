import {useDispatch} from 'react-redux'

import {FormControl,FormLabel,Card} from 'react-bootstrap'

const Customer = ({name,food,id}) => {

    const dispatch = useDispatch();

    return ( 
        <div>

        </div>
     );
}
 
export default Customer;